# URL of the Configuration Service (server-side only, not exposed to the browser)
CONFIGURATION_URL=http://localhost:8080

# URL of the Order Service (server-side only, not exposed to the browser)
ORDER_URL=http://localhost:8081
